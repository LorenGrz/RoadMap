// Operadores

// Operadores aritmeticos
let a = 5
let b = 10
console.log(a + b)
/*
    + = Suma
    - = resta
    * = Multiplicacion
    / = Division
    % = Modulo
    ** = Exponente
*/

// ++ Incremento
// -- Decremento

// Operadores de comparacion
// > es mayor a
// < es menor a
// >= es mayor o igual a
// <= es menor o igual a
// == es igual a
// === es identico (tipo y valor) a
// != es distinto a
// !== es distinto (tipo y valor) a

// Operadores logicos
// and (&&)
// or (||)
// not (!)

// Operadores ternarios

const isRaning = true

isRaning ? console.log("Esta lloviendo") : console.log("No esta lloviendo")
